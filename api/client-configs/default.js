export default function(context) {
    return {
        httpEndpoint: '/graphql',
        httpLinkOptions: {
            headers: {
                'Authorization': '강민규천재'
            }
        }
    }
}